{
    "title": "Configure - name based input",
    "description": "Edit the configuration of your name based input",
    "type": "object",
    "required":["name", "prefix"],
    "properties": {
        "name":{
            "type":"string"
        },
        "prefix":{
            "type":"string",
            "description":"the prefix that has to be used when passed to the command"
        }

    }
}